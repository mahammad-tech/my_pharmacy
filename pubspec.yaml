name: my_pharmacy
description: A new Flutter pharmacy app.
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.0 # State management
  hive: ^2.0.0       # Local storage
  hive_flutter: ^1.0.0 # Flutter integration for Hive

flutter:
  uses-material-design: true
